/target/
/artifacts/
/.sisyphus/

# External corpus source trees are fetched on demand; never committed.
# See corpus/manifest.toml and scripts/fetch-corpus.sh.
corpus/programs/
/.commandcode/
/.antigravitycli/
/node_modules/
.DS_Store
*.tsbuildinfo

# Fuzz runs expand target-local corpora. Keep only checked-in seed fixtures.
fuzz/corpus/fuzz_anchor_preflight/*
!fuzz/corpus/fuzz_anchor_preflight/basic.bin

# Editor build artifacts (not published)
editors/vscode/dist/

# Local assistant/editor activation files. Tracked templates live in editors/.
/opencode.json
/.cursor/
